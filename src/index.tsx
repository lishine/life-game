import * as React from 'react'
import { render } from 'react-dom'

import { ThemeWrapper } from './ThemeWrapper'

const rootElement = document.getElementById('root')

render(<ThemeWrapper />, rootElement)
